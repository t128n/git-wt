{
  "name": "@t128n/git-wt",
  "version": "1.0.0",
  "description": "A simpler interface to git worktrees",
  "license": "MIT",
  "private": true,
  "scripts": {
    "version-packages": "changeset version && cargo set-version $(bun -e \"console.log(require('./package.json').version)\")"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
